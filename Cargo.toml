[workspace]

members = [
    "brick",
    "brain",
    "predict",
    "state",
    "passthrough",
    "record",
    "generate-data",
    "heuristic",
]

[profile.dev]
opt-level = 2

[profile.release]
lto = 'thin'
codegen-units = 1
